package com.github.utils;

import com.github.constant.CaseEnum;

/**
 * @author Yan
 */
public class StringHelper {
    public static String caseTypeConvert(String caseStr, CaseEnum form, CaseEnum to) {
        for (char item : caseStr.toCharArray()) {
            if (form == CaseEnum.CAMEL_CASE) {
            }
        }
        return "";
    }
}
